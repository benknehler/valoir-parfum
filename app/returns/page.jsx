import InfoPage from '../../components/InfoPage.jsx';

export const metadata = {
  title: 'Returns | Valoir Parfum',
};

export default function ReturnsPage() {
  return <InfoPage slug="returns" />;
}
